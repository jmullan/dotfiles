#!/bin/bash
GIT_DIR=`git rev-parse --git-dir 2>/dev/null`
if [ -z "$GIT_DIR" ] ; then
    exit 1
fi
TRACKING=`git track`
if [ -z "$TRACKING" ] ; then
    exit 1
fi
git rev-list --left-right "HEAD...$TRACKING" | grep '^>' | wc -l | awk '{print $1}'
