#!/usr/bin/env php
<?php
$total = 0;
while (!feof(STDIN)) {
    $line = trim(fgets(STDIN));
    $parts = preg_split('/\s+/', $line);
    if ($parts && strlen($parts[0]) && is_numeric($parts[0])) {
	$total += $line;
    }
}
echo "$total\n";
