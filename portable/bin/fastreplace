#!/bin/bash
if [ -z "$1" ] ; then
    echo "No first argument";
    exit 1;
fi
if [ -z "$2" ] ; then
    echo "No second argument";
    exit 1;
fi
recursiveReplace.php "$1" "$2" `grep -rl "$1" . | grep -v '.bak$' | grep -v .git | grep -v .pyc | grep -v nosetests.xml`
